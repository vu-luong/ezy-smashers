<?xml version="1.0" encoding="UTF-8"?>
<project name="EzySmashers-app-api" default="install" basedir=".">
	<import file="../build.xml" />

	<target name="install">
		<copy todir="${ezyfox.home.apps.common}">
			<fileset dir="target">
				<filename name="${project.name}-${project.version}.jar" />
			</fileset>
		</copy>
	</target>

</project>